spring:
  datasource:
    url: jdbc:mysql://localhost:3306/brokerage?useSSL=true&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
  sql:
    init:
      mode: always